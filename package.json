{
	"name": "sheets-sync",
	"version": "1.0.0",
	"description": "Bidirectionally sync a markdown table with a Google Sheets range.",
	"main": "main.js",
	"scripts": {
		"dev": "node esbuild.config.mjs",
		"build": "tsc -noEmit -skipLibCheck && node esbuild.config.mjs production",
		"format": "prettier --write ."
	},
	"keywords": [],
	"author": "",
	"license": "MIT",
	"devDependencies": {
		"@types/node": "^16.3.0",
		"builtin-modules": "^3.3.0",
		"esbuild": "^0.17.3",
		"obsidian": "latest",
		"prettier": "^3.9.8",
		"typescript": "^4.7.4"
	}
}
